# Create by MXuDong
# To describe the rule file.

# Todo: create the rules.
root:
  type: map
  elements:
    - template

template:
  type: map
  elements:
    - name
    - hocks:
        require: False
        type: map
        elements:
          - name
          - test
    - body:
        require: False
    - refs
test:
  type: string
name:
  type: string
  elements:
    - test